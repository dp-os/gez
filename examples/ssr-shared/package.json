{
  "name": "ssr-shared",
  "version": "2.0.1-alpha.44",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "dev": "ts-node genesis.dev -p=tsconfig.json",
    "build": "npm run build:dts && rm -rf dist && NODE_ENV=production ts-node genesis.build -p=tsconfig.json",
    "build:dts": "vue-tsc --declaration --emitDeclarationOnly",
    "start": "NODE_ENV=production ts-node genesis.prod -p=tsconfig.json"
  },
  "devDependencies": {
    "@fmfe/genesis-compiler": "2.0.1-alpha.44",
    "@types/express": "^4.17.13",
    "element-ui": "^2.15.7",
    "vue": "^2.6.14",
    "vue-meta": "^2.4.0",
    "vue-router": "^3.5.3"
  },
  "dependencies": {
    "@fmfe/genesis-core": "2.0.1-alpha.44",
    "express": "^4.17.2",
    "ts-node": "^10.5.0"
  }
}
