---
titleSuffix: Gez フレームワーク Vue ビルドツール
description: Gez フレームワークの Vue 専用ビルドツールで、Vue 2/3 アプリケーションのビルドを完全にサポートします。コンポーネント開発、SSR レンダリング、パフォーマンス最適化などの機能を提供します。
head:
  - - meta
    - property: keywords
      content: Gez, Rspack, Vue, Vue2, Vue3, SSR, ビルドツール, コンポーネント開発, サーバーサイドレンダリング, パフォーマンス最適化
---

import { PackageManagerTabs } from '@theme';

# @gez/rspack-vue

Rspack Vue パッケージは、Vue フレームワークに基づく Rspack アプリケーションを作成および設定するための API を提供し、Vue コンポーネントの開発、ビルド、およびサーバーサイドレンダリングをサポートします。

## インストール

パッケージマネージャーを使用して `@gez/rspack-vue` 開発依存関係をインストールします：

<PackageManagerTabs command="install @gez/rspack-vue -D" />

## 型エクスポート

### BuildTarget

```ts
type BuildTarget = 'node' | 'client' | 'server'
```

ビルドターゲット環境の型で、アプリケーションのビルドターゲット環境を定義し、ビルドプロセス中の特定の最適化と機能を設定するために使用されます：
- `node`: Node.js 環境で実行されるコードをビルド
- `client`: ブラウザ環境で実行されるコードをビルド
- `server`: サーバー環境で実行されるコードをビルド

### RspackAppConfigContext

```ts
interface RspackAppConfigContext {
  gez: Gez
  buildTarget: BuildTarget
  config: RspackOptions
  options: RspackAppOptions
}
```

Rspack アプリケーション設定コンテキストインターフェースで、設定フック関数内でアクセス可能なコンテキスト情報を提供します：
- `gez`: Gez フレームワークインスタンス
- `buildTarget`: 現在のビルドターゲット（client/server/node）
- `config`: Rspack 設定オブジェクト
- `options`: アプリケーション設定オプション

### RspackAppOptions

```ts
interface RspackAppOptions {
  css?: 'css' | 'style'
  loaders?: {
    styleLoader?: string
  }
  styleLoader?: Record<string, any>
  cssLoader?: Record<string, any>
  target?: {
    web?: string[]
    node?: string[]
  }
  definePlugin?: Record<string, any>
  config?: (context: RspackAppConfigContext) => void | Promise<void>
}
```

Rspack アプリケーション設定オプションインターフェース：
- `css`: CSS 出力方式、'css'（独立ファイル）または 'style'（インラインスタイル）を選択可能
- `loaders`: カスタム loader 設定
- `styleLoader`: style-loader 設定オプション
- `cssLoader`: css-loader 設定オプション
- `target`: ビルドターゲット互換性設定
- `definePlugin`: グローバル定数定義
- `config`: 設定フック関数

### RspackHtmlAppOptions

`RspackAppOptions` を継承し、HTML アプリケーションの特定のオプションを設定するために使用されます。

## 関数エクスポート

### createRspackApp

```ts
function createRspackApp(gez: Gez, options?: RspackAppOptions): Promise<App>
```

標準の Rspack アプリケーションインスタンスを作成します。

**パラメータ：**
- `gez`: Gez フレームワークインスタンス
- `options`: Rspack アプリケーション設定オプション

**戻り値：**
- 作成されたアプリケーションインスタンスを解決する Promise を返します

### createRspackHtmlApp

```ts
function createRspackHtmlApp(gez: Gez, options?: RspackHtmlAppOptions): Promise<App>
```

HTML タイプの Rspack アプリケーションインスタンスを作成します。

**パラメータ：**
- `gez`: Gez フレームワークインスタンス
- `options`: HTML アプリケーション設定オプション

**戻り値：**
- 作成された HTML アプリケーションインスタンスを解決する Promise を返します

## 定数エクスポート
### RSPACK_LOADER

```ts
const RSPACK_LOADER: Record<string, string> = {
  builtinSwcLoader: 'builtin:swc-loader',
  lightningcssLoader: 'builtin:lightningcss-loader',
  styleLoader: 'style-loader',
  cssLoader: 'css-loader',
  lessLoader: 'less-loader',
  styleResourcesLoader: 'style-resources-loader',
  workerRspackLoader: 'worker-rspack-loader'
}
```

Rspack に組み込まれた loader 識別子マッピングオブジェクトで、一般的に使用される loader 名の定数を提供します：

- `builtinSwcLoader`: TypeScript/JavaScript ファイルを処理するための Rspack 組み込みの SWC loader
- `lightningcssLoader`: CSS ファイルを処理するための高性能コンパイラである Rspack 組み込みの lightningcss loader
- `styleLoader`: CSS を DOM に注入するための loader
- `cssLoader`: CSS ファイルを解析し、CSS モジュール化を処理するための loader
- `lessLoader`: Less ファイルを CSS にコンパイルするための loader
- `styleResourcesLoader`: グローバルスタイルリソース（変数、mixins など）を自動的にインポートするための loader
- `workerRspackLoader`: Web Worker ファイルを処理するための loader

これらの定数を使用して、設定内で組み込みの loader を参照し、手動で文字列を入力することを避けることができます：

```ts title="src/entry.node.ts"
import { RSPACK_LOADER } from '@gez/rspack';

export default {
  async devApp(gez) {
    return import('@gez/rspack').then((m) =>
      m.createRspackHtmlApp(gez, {
        loaders: {
          // 定数を使用して loader を参照
          styleLoader: RSPACK_LOADER.styleLoader,
          cssLoader: RSPACK_LOADER.cssLoader,
          lightningcssLoader: RSPACK_LOADER.lightningcssLoader
        }
      })
    );
  }
};
```

**注意事項：**
- これらの loader は Rspack に組み込まれており、追加のインストールは不要です
- カスタム loader 設定を行う際に、これらの定数を使用してデフォルトの loader 実装を置き換えることができます
- 一部の loader（例：`builtinSwcLoader`）には特定の設定オプションがありますので、対応する設定ドキュメントを参照してください

## モジュールエクスポート

### rspack

`@rspack/core` パッケージのすべての内容を再エクスポートし、完全な Rspack コア機能を提供します。