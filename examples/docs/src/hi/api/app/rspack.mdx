---
titleSuffix: Gez फ्रेमवर्क Rspack बिल्ड टूल
description: Gez फ्रेमवर्क का Rspack बिल्ड टूल, उच्च प्रदर्शन वाले एप्लिकेशन बिल्ड क्षमता प्रदान करता है, स्टैंडर्ड एप्लिकेशन और HTML एप्लिकेशन के विकास और बिल्ड को सपोर्ट करता है, अंतर्निहित कई संसाधन प्रोसेसर और ऑप्टिमाइज़ेशन कॉन्फ़िगरेशन।
head:
  - - meta
    - property: keywords
      content: Gez, Rspack, बिल्ड टूल, एप्लिकेशन बिल्ड, HTML एप्लिकेशन, TypeScript, CSS, संसाधन प्रसंस्करण, प्रदर्शन अनुकूलन
---

import { PackageManagerTabs } from '@theme';

# @gez/rspack

Rspack पैकेज Rspack एप्लिकेशन बनाने और कॉन्फ़िगर करने के लिए एक API सेट प्रदान करता है, जो स्टैंडर्ड एप्लिकेशन और HTML एप्लिकेशन के बिल्ड और विकास को सपोर्ट करता है।

## इंस्टॉलेशन

पैकेज मैनेजर का उपयोग करके `@gez/rspack` डेवलपमेंट डिपेंडेंसी इंस्टॉल करें:

<PackageManagerTabs command="install @gez/rspack -D" />

## टाइप एक्सपोर्ट

### BuildTarget

```ts
type BuildTarget = 'node' | 'client' | 'server'
```

बिल्ड टार्गेट एनवायरनमेंट टाइप, एप्लिकेशन के बिल्ड टार्गेट एनवायरनमेंट को परिभाषित करता है, जिसका उपयोग बिल्ड प्रक्रिया में विशिष्ट ऑप्टिमाइज़ेशन और फीचर्स को कॉन्फ़िगर करने के लिए किया जाता है:
- `node`: Node.js एनवायरनमेंट में चलने वाले कोड के लिए बिल्ड
- `client`: ब्राउज़र एनवायरनमेंट में चलने वाले कोड के लिए बिल्ड
- `server`: सर्वर एनवायरनमेंट में चलने वाले कोड के लिए बिल्ड

### RspackAppConfigContext

```ts
interface RspackAppConfigContext {
  gez: Gez
  buildTarget: BuildTarget
  config: RspackOptions
  options: RspackAppOptions
}
```

Rspack एप्लिकेशन कॉन्फ़िगरेशन कॉन्टेक्स्ट इंटरफ़ेस, कॉन्फ़िगरेशन हुक फ़ंक्शन में एक्सेस किए जा सकने वाले कॉन्टेक्स्ट जानकारी प्रदान करता है:
- `gez`: Gez फ्रेमवर्क इंस्टेंस
- `buildTarget`: वर्तमान बिल्ड टार्गेट (client/server/node)
- `config`: Rspack कॉन्फ़िगरेशन ऑब्जेक्ट
- `options`: एप्लिकेशन कॉन्फ़िगरेशन विकल्प

### RspackAppOptions

```ts
interface RspackAppOptions {
  css?: 'css' | 'style'
  loaders?: {
    styleLoader?: string
  }
  styleLoader?: Record<string, any>
  cssLoader?: Record<string, any>
  target?: {
    web?: string[]
    node?: string[]
  }
  definePlugin?: Record<string, any>
  config?: (context: RspackAppConfigContext) => void | Promise<void>
}
```

Rspack एप्लिकेशन कॉन्फ़िगरेशन विकल्प इंटरफ़ेस:
- `css`: CSS आउटपुट मोड, वैकल्पिक 'css' (अलग फ़ाइल) या 'style' (इनलाइन स्टाइल)
- `loaders`: कस्टम लोडर कॉन्फ़िगरेशन
- `styleLoader`: style-loader कॉन्फ़िगरेशन विकल्प
- `cssLoader`: css-loader कॉन्फ़िगरेशन विकल्प
- `target`: बिल्ड टार्गेट कम्पेटिबिलिटी कॉन्फ़िगरेशन
- `definePlugin`: ग्लोबल कॉन्स्टेंट डेफ़िनिशन
- `config`: कॉन्फ़िगरेशन हुक फ़ंक्शन

### RspackHtmlAppOptions

`RspackAppOptions` से इनहेरिट करता है, HTML एप्लिकेशन के लिए विशिष्ट विकल्प कॉन्फ़िगर करने के लिए उपयोग किया जाता है।

## फ़ंक्शन एक्सपोर्ट

### createRspackApp

```ts
function createRspackApp(gez: Gez, options?: RspackAppOptions): Promise<App>
```

एक स्टैंडर्ड Rspack एप्लिकेशन इंस्टेंस बनाता है।

**पैरामीटर्स:**
- `gez`: Gez फ्रेमवर्क इंस्टेंस
- `options`: Rspack एप्लिकेशन कॉन्फ़िगरेशन विकल्प

**रिटर्न वैल्यू:**
- एक Promise रिटर्न करता है, जो बनाए गए एप्लिकेशन इंस्टेंस को रिज़ॉल्व करता है

### createRspackHtmlApp

```ts
function createRspackHtmlApp(gez: Gez, options?: RspackHtmlAppOptions): Promise<App>
```

एक HTML प्रकार का Rspack एप्लिकेशन इंस्टेंस बनाता है।

**पैरामीटर्स:**
- `gez`: Gez फ्रेमवर्क इंस्टेंस
- `options`: HTML एप्लिकेशन कॉन्फ़िगरेशन विकल्प

**रिटर्न वैल्यू:**
- एक Promise रिटर्न करता है, जो बनाए गए HTML एप्लिकेशन इंस्टेंस को रिज़ॉल्व करता है

## कॉन्स्टेंट एक्सपोर्ट
### RSPACK_LOADER

```ts
const RSPACK_LOADER: Record<string, string> = {
  builtinSwcLoader: 'builtin:swc-loader',
  lightningcssLoader: 'builtin:lightningcss-loader',
  styleLoader: 'style-loader',
  cssLoader: 'css-loader',
  lessLoader: 'less-loader',
  styleResourcesLoader: 'style-resources-loader',
  workerRspackLoader: 'worker-rspack-loader'
}
```

Rspack में अंतर्निहित लोडर आइडेंटिफ़ायर मैपिंग ऑब्जेक्ट, सामान्यतः उपयोग किए जाने वाले लोडर नाम कॉन्स्टेंट प्रदान करता है:

- `builtinSwcLoader`: Rspack में अंतर्निहित SWC लोडर, TypeScript/JavaScript फ़ाइलों को प्रोसेस करने के लिए
- `lightningcssLoader`: Rspack में अंतर्निहित lightningcss लोडर, CSS फ़ाइलों के लिए उच्च प्रदर्शन कंपाइलर
- `styleLoader`: CSS को DOM में इंजेक्ट करने के लिए लोडर
- `cssLoader`: CSS फ़ाइलों को पार्स करने और CSS मॉड्यूलराइज़ेशन को प्रोसेस करने के लिए लोडर
- `lessLoader`: Less फ़ाइलों को CSS में कंपाइल करने के लिए लोडर
- `styleResourcesLoader`: ग्लोबल स्टाइल संसाधन (जैसे वेरिएबल्स, mixins) को ऑटोमेटिकली इम्पोर्ट करने के लिए लोडर
- `workerRspackLoader`: Web Worker फ़ाइलों को प्रोसेस करने के लिए लोडर

इन कॉन्स्टेंट का उपयोग करके कॉन्फ़िगरेशन में अंतर्निहित लोडर को रेफ़र कर सकते हैं, मैन्युअल स्ट्रिंग इनपुट से बच सकते हैं:

```ts title="src/entry.node.ts"
import { RSPACK_LOADER } from '@gez/rspack';

export default {
  async devApp(gez) {
    return import('@gez/rspack').then((m) =>
      m.createRspackHtmlApp(gez, {
        loaders: {
          // कॉन्स्टेंट का उपयोग करके लोडर को रेफ़र करें
          styleLoader: RSPACK_LOADER.styleLoader,
          cssLoader: RSPACK_LOADER.cssLoader,
          lightningcssLoader: RSPACK_LOADER.lightningcssLoader
        }
      })
    );
  }
};
```

**ध्यान दें:**
- ये लोडर Rspack में पहले से अंतर्निहित हैं, अतिरिक्त इंस्टॉलेशन की आवश्यकता नहीं है
- कस्टम लोडर कॉन्फ़िगरेशन करते समय, इन कॉन्स्टेंट का उपयोग करके डिफ़ॉल्ट लोडर इम्प्लीमेंटेशन को रिप्लेस कर सकते हैं
- कुछ लोडर (जैसे `builtinSwcLoader`) के विशिष्ट कॉन्फ़िगरेशन विकल्प होते हैं, संबंधित कॉन्फ़िगरेशन डॉक्यूमेंटेशन देखें

## मॉड्यूल एक्सपोर्ट

### rspack

`@rspack/core` पैकेज की सभी सामग्री को री-एक्सपोर्ट करता है, Rspack कोर फ़ंक्शनलिटी प्रदान करता है।