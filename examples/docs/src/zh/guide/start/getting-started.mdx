---
titleSuffix: Gez 框架快速入门指南
description: 从零开始搭建基于 Gez 的 SSR 应用，通过实例展示框架的基本用法，包括项目初始化、TypeScript 配置和入口文件设置。
head:
  - - meta
    - property: keywords
      content: Gez, SSR应用, TypeScript配置, 项目初始化, 服务端渲染, 客户端交互, 开发环境配置
---

# 快速开始

本教程将帮助你从零开始搭建一个基于 Gez 的 SSR 应用。我们将通过一个简单的示例来展示 Gez 的基本用法。

## 项目初始化

首先，让我们创建一个新的项目并配置必要的依赖。创建 `package.json` 文件：

```json
{
  "name": "your-app-name",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "gez dev",
    "build": "npm run build:dts && npm run build:ssr",
    "build:ssr": "gez build",
    "preview": "gez preview",
    "start": "NODE_ENV=production node dist/index.js",
    "build:dts": "tsc --declaration --emitDeclarationOnly --outDir dist/src"
  },
  "dependencies": {
    "@gez/core": "*"
  },
  "devDependencies": {
    "@gez/rspack": "*",
    "@types/node": "22.8.6",
    "typescript": "^5.2.2"
  }
}
```

创建完 `package.json` 文件后，需要安装项目依赖。你可以使用以下任一命令来安装：
```bash
pnpm add @gez/core
# 或
yarn add @gez/core
# 或
npm install @gez/core
```

## TypeScript 配置

创建 `tsconfig.json` 文件，配置 TypeScript 编译选项：

```json
{
    "compilerOptions": {
        "module": "ESNext",
        "moduleResolution": "node",
        "isolatedModules": true,
        "resolveJsonModule": true,
        
        "target": "ESNext",
        "lib": ["ESNext", "DOM"],
        
        "strict": true,
        "skipLibCheck": true,
        "types": ["@types/node"],
        
        "allowSyntheticDefaultImports": true,
        
        "baseUrl": ".",
        "paths": {
            "your-app-name/src/*": ["./src/*"],
            "your-app-name/*": [".*"]
        }
    },
    "include": ["src"],
    "exclude": ["dist", "node_modules"]
}
```

## 入口文件

### 服务端

创建 `src/entry.node.ts` 文件，配置服务器和开发环境：

```typescript
/**
 * @file Node.js 服务器入口文件
 * @description 负责开发环境配置和服务器启动，提供 SSR 运行时环境
 */

import http from 'node:http';
import type { GezOptions } from '@gez/core';

export default {
    /**
     * 配置开发环境的应用创建器
     * @description 创建并配置 Rspack 应用实例，用于开发环境的构建和热更新
     * @param gez Gez 框架实例，提供核心功能和配置接口
     * @returns 返回配置好的 Rspack 应用实例，支持 HMR 和实时预览
     */
    async devApp(gez) {
        return import('@gez/rspack').then((m) =>
            m.createRspackHtmlApp(gez, {
                config(context) {
                    // 在此处自定义 Rspack 编译配置
                }
            })
        );
    },

    /**
     * 配置并启动 HTTP 服务器
     * @description 创建 HTTP 服务器实例，集成 Gez 中间件，处理 SSR 请求
     * @param gez Gez 框架实例，提供中间件和渲染功能
     */
    async server(gez) {
        const server = http.createServer((req, res) => {
            // 使用 Gez 中间件处理请求
            gez.middleware(req, res, async () => {
                // 执行服务端渲染
                const rc = await gez.render({
                    params: { url: req.url }
                });
                res.end(rc.html);
            });
        });

        server.listen(3000, () => {
            console.log('服务启动: http://localhost:3000');
        });
    }
} satisfies GezOptions;
```

### 服务端渲染

创建 `src/entry.server.ts` 文件，实现服务端渲染逻辑：

```typescript
/**
 * @file 服务端渲染入口文件
 * @description 负责服务端渲染流程、HTML 生成和资源注入
 */

import type { RenderContext } from '@gez/core';

// 模拟 renderToString 函数，生成页面内容
const renderToString = () => {
    const time = new Date().toISOString();
    return `
    <h1><a href="https://www.gez-esm.com/guide/start/getting-started.html" target="_blank">Gez 快速开始</a></h1>
    <time datetime="${time}">${new Date(time).toLocaleString('zh-CN')}</time>
    `;
};

export default async (rc: RenderContext) => {
    // 使用 renderToString 生成页面内容
    const html = renderToString();

    // 提交依赖收集，确保所有必要资源都被加载
    await rc.commit();

    // 生成完整的 HTML 结构
    rc.html = `<!DOCTYPE html>
<html lang="zh-CN">
<head>
    ${rc.preload()}
    <title>Gez 快速开始</title>
    ${rc.css()}
</head>
<body>
    ${html}
    ${rc.importmap()}
    ${rc.moduleEntry()}
    ${rc.modulePreload()}
</body>
</html>
`;
};
```

### 客户端

创建 `src/entry.client.ts` 文件，实现客户端交互逻辑：

```typescript
/**
 * @file 客户端入口文件
 * @description 负责客户端交互逻辑和动态更新
 */

// 获取时间显示元素并实现自动更新
const time = document.querySelector('time');
setInterval(() => {
    // 使用 ISO 格式更新时间显示
    time?.setHTMLUnsafe(new Date().toISOString());
}, 1000);
```

## 运行项目

完成上述文件配置后，你可以使用以下命令来运行项目：

1. 开发模式：
```bash
npm run dev
```

2. 构建项目：
```bash
npm run build
```

3. 生产环境运行：
```bash
npm run start
```

现在，你已经成功创建了一个基于 Gez 的 SSR 应用！访问 http://localhost:3000 即可看到效果。
