{
  "name": "ssr-demo",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "genesis-ts-node genesis.dev -p=tsconfig.json",
    "build": "npm run build:dts && rm -rf dist && NODE_ENV=production genesis-ts-node genesis.build -p=tsconfig.json",
    "build:dts": "genesis-vue-tsc --declaration --emitDeclarationOnly",
    "start": "NODE_ENV=production genesis-ts-node genesis.prod -p=tsconfig.json"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fmfe/genesis-core": "^2.0.4",
    "express": "^4.17.3"
  },
  "devDependencies": {
    "@fmfe/genesis-compiler": "^2.0.4",
    "@types/express": "^4.17.13"
  }
}
